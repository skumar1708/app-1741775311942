const Game = require('../models/Game'); exports.createGame = async (req, res) => { const newGame = new Game(); await newGame.save(); res.status(201).json(newGame); }; exports.getGame = async (req, res) => { const game = await Game.findById(req.params.id); if (!game) return res.status(404).json({ msg: 'Game not found' }); res.json(game); }; exports.updateGame = async (req, res) => { const game = await Game.findByIdAndUpdate(req.params.id, req.body, { new: true }); if (!game) return res.status(404).json({ msg: 'Game not found' }); res.json(game); };